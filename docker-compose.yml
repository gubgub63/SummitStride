version: '3.8'

services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: coach-ia-hugo-postgres
    restart: always
    environment:
      POSTGRES_DB: coach_ia_hugo_dev
      POSTGRES_USER: coach_user
      POSTGRES_PASSWORD: coach_password_dev
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U coach_user -d coach_ia_hugo_dev']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: coach-ia-hugo-redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
